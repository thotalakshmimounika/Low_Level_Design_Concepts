package Interface.examples;

public interface A {
    void a();
    void b();

}
//interface : it is used to when we want few classes to inherite the methods from like a parent class. Here paraent class is the
//interface class that has no attributes only methods without body. So whenever a class implements a interface class it should override those methods with the body whatever it required